## How to run in Aristotelis Cluster

1. First compile by running the `compile.sh` script
2. Submit batch job by using `sbatch run.sh` script

General usage is `./gpu <N_BLOCKS> <N_THREADS_PER_BLOCK> <FILENAME>`